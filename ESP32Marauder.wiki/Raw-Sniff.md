# Raw Sniff
Used to capture all WiFi frames and store as PCAP. Displays only statistics and prioritized compute power for capturing and storing data. Use this function specifically for capturing all data to be analyzed in post.

## Menu Path
`WiFi`>`Sniffers`>`Raw Sniff`