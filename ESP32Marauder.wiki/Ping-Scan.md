# Ping Scan
Perform a full ICMP ping sweep of the full IP address range available on the currently connected network. The IP range is based on the gateway and subnet mask of the network. IPs which respond to the ICMP request are stored in a list of active IP addresses.

## CLI Usage
`pingscan`